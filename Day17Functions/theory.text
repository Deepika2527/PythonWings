Functions:
Function is a block of code for repetative task, by calling it

print("Hello all")
print("Hello all")
print("Hello all")
print("Hello all")
print("Hello all")
--------------------------------------------------------------------------------------
syntax of function:
def function_name():
    statements
function_name()

def - define a function
function_name: snake_case - test(),display(),maths(),greet(), please follow the identifier rules
() : placeholder
: - This define the block and indentation
statemnets - print()

function_name() - calling a function

--------------------------------------------------------------------------
Type of Functions
1. Buitin Functions
2. User defined Functions



1. Built in functions :
These fucntions has a special meaning, where it is pre-defined by the python
print()
len()
range()
enumerate()
list()


How can we know the buitin functions in python
dir(__buitins__)

dir - This will list out all the methods of that object
builtin - Special object that can contains all the fucntions
__ - magicnames/specialnames


2. User defined functions :
These are define by the user based on the logic

Types of user defined functions
1. Basic function
    a. function without return statemnets
    b. function with return statemnets:
       * After retun if we write the statement it will not execute
       * If we dont use return and tries to print the function using assigning to a varaibale then it return None
       * return will returns tuple with mutiple operations
2. Functional Argument
     a. Positional arguments
        These are the values that are being passed as a aruguments for the parameter based on the postition
        def add(p1,p2):
           #statements
        add(a1,a2)
     b. keyword arguments :
     These are the functions that are being passed as a argumenets based on the keywords.
     c. Default arguments - task
     d . Varaible length positional arguments - *arguments
     when we want to pass so many as argumenst we use *arg
     this will return tuple
     e . Keyword varaible length arguments - **kwargs
     This will return the dict
     values should be passed in key-values
     synatx:
     def test(**kwargs):
          statement
      test(k='v',k1='v1')



keytakeAways:
When we work with both postional and keyword arguments, first positional arguments then keyword arguments






# task1 onbvert tupel into list
#  task 2 how doe unpackin g works with args